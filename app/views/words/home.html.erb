<%=simple_form_for @word, url: {action: "home"}, html: {class: "form-horizontal"} do |f|  %>
	<%= f.label @word.name%>
	<%= f.input :answer, collection: @array, as: :radio_buttons%>
	<%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
